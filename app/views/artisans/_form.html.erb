<%= form_for(@artisan) do |f| %>
  <% if @artisan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artisan.errors.count, "error") %> prohibited this artisan from being saved:</h2>

      <ul>
      <% @artisan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="field">
    <%= f.label :approved %><br>
    <%= f.check_box :approved %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
