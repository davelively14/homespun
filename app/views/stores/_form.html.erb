<%= form_for(@store) do |f| %>
  <% if @store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <%= f.text_field :name, autofocus: true, required: true, placeholder: 'Store Name'  %>
    <%= f.text_area :description, required: true, placeholder: 'Store Description' %>
    <%= f.submit 'Submit', class: 'button radius large expand' %>
  </div>
<% end %>
