<%= form_for(@store_user) do |f| %>
  <% if @store_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_user.errors.count, "error") %> prohibited this store_user from being saved:</h2>

      <ul>
      <% @store_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
